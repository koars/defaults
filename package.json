{
  "name": "koars-defaults",
  "version": "1.0.0",
  "main": "index.js",
  "description": "Provides default settings for the koars framework",
  "repository": {
    "type": "git",
    "url": "https://github.com/koars/defaults.git"
  },
  "devDependencies": {
    "jshint": "^2.5.6",
    "jshint-stylish": "^1.0.0",
    "mocha": "^1.21.4",
    "must": "^0.12.0",
    "sinon": "^1.10.3"
  },
  "scripts": {
    "test": "mocha --require must && jshint . --exclude-path .gitignore --reporter node_modules/jshint-stylish/stylish.js"
  },
  "author": "Florian Albertz",
  "license": "BSD-3-Clause",
  "jshintConfig": {
    "node": true,
    "mocha": true
  }
}
